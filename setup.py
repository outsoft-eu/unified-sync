from setuptools import setup, find_packages

setup(
    name="unified_sync",
    version="0.1",
    packages=find_packages(),
    install_requires=[],
    python_requires=">=3.7",
    author="BI-team",
    description="A universal project for rapid ETL process development",
)